<p id="notice"><%= notice %></p>

<h1><%= @band.name %></h1>

<h2>Albums</h2>

<ul>
  <% @albums.each do |album| %>
  <li><%= link_to album.title, album %>, <%= album.year %> (<%= album.album_type %>)

    </li>
  <% end %>
</ul>



<%= link_to 'Edit this band', edit_band_path(@band) %>

<%= link_to 'Back to bands list', bands_path %>
